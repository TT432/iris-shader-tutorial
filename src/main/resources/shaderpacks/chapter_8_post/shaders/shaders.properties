# Chapter 8 Post-processing Shaderpack
# Each preset enables a different post-processing effect for screenshot comparison.
# The mod injects EFFECT=<name> via #define injection before Iris reload.

screen.EFFECT=bloom
sliders=EFFECT
options=EFFECT

# Default is pass-through (no effect)
# Presets: bloom, tonemap, colorgrade, fxaa, filmlook
